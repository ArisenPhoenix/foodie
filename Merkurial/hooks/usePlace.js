import { useContext } from "react";
import MerkurialContext from "../FlexBox/MerkurialFlexBoxContext";

export const usePlace = (xStart, xEnd, yStart, yEnd) => {
  const merkCtx = useContext(MerkurialContext);
  merkCtx;
};

export const useMerge = (x, y) => {};
